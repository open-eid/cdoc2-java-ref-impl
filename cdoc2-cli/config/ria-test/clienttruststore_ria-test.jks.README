keytool -import -trustcacerts -file tls-issuer.crt.pem -alias klass3-ria_2018_ecc_g3 -storepass passwd -keystore clienttruststore_ria-test.jks

keytool -import -file cdoc2-shares_test_riaint_ee.crt.pem -storepass passwd -keystore clienttruststore_ria-test.jks -alias share1

keytool -import -file cdoc2-sharesexternal_test_riaint_ee.crt.pem -storepass passwd -keystore clienttruststore_ria-test.jks -alias share2
