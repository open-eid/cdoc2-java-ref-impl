include "recipients.fbs";

namespace ee.cyber.cdoc20.fbs.server;

// Union for communicating only the recipient types server can output.
union Recipient { Recipients.ECCPublicKey }

// Server response structure
table ServerResponse {
    recipient:Recipient;
}

root_type ServerResponse;
