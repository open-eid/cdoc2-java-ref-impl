# To use variables from here, run `source export-env.sh .env.local`
# and then execute `docker compose up` or run bats tests as usual

# docker compose variables for locally built images
# To build locally, run `cd cdoc2-shares-server && ./build-images.sh`
# and update SHARES_SERVER_VERSION in this file


DOCKER_REGISTRY=ghcr.io
DOCKER_REPOSITORY=open-eid

SHARES_SERVER_VERSION=latest
